<Window x:Class="RepairRequests.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RepairRequests"
        mc:Ignorable="d"
        Title="RegisterWindow" Height="206" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Логин:" Grid.Row="0" Grid.Column="0"/>
        <TextBox x:Name="LoginTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <Label Content="Пароль:" Grid.Row="1" Grid.Column="0"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Label Content="Подтвердите пароль:" Grid.Row="2" Grid.Column="0"/>
        <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <Button Content="Зарегистрироваться" Click="RegisterButton_Click" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Right" Width="120" Margin="5"/>
    </Grid>
</Window>